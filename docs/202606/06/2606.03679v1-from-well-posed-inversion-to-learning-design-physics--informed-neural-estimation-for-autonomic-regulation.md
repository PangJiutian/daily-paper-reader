---
title: "From Well-Posed Inversion to Learning Design: Physics- Informed Neural Estimation for Autonomic Regulation"
title_zh: 从适定反演到学习设计：用于自主调节的物理信息神经估计
authors: "Sara Nour Sadoun, Giuseppe Alessio D'Inverno, Francois Cottin, Arnaud Boutin, Taous-Meriem Laleg-Kirati"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03679v1"
tags: ["query:geo-phys-ai"]
score: 8.0
evidence: 将适定性约束融入设计的物理信息神经估计器
tldr: 现有基于学习的逆估计方法常忽略Hadamard适定性条件，导致未知输入重建不可靠。本文提出一种物理信息输入-状态神经估计器，通过微分代数消除导出左可逆性条件并嵌入训练目标，同时分析映射的Lipschitz稳定性指导超参数选择。在自主心脏调节模型上的模拟和真实心血管数据实验中，该方法相比仅前向一致性约束显著提升了逆推理的可靠性。工作表明融入控制理论可解性约束是提升物理信息学习逆估计可信度的有效途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03679v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 683, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03679v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1673, \"height\": 865, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03679v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1812, \"height\": 618, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03679v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1747, \"height\": 582, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03679v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1830, \"height\": 773, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03679v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1808, \"height\": 381, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03679v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1759, \"height\": 386, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03679v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 824, \"height\": 402, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03679v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03679v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1050, \"height\": 239, \"label\": \"Table\"}]"
motivation: 现有方法忽视Hadamard适定性条件，导致逆估计不可靠。
method: 通过微分代数消除导出左可逆性条件，嵌入训练目标并分析Lipschitz界。
result: 在模拟和真实心血管数据上，融入约束的估计器比仅前向一致性更可靠。
conclusion: 控制理论可解性约束可提升物理信息学习的逆推理可靠性。
---

## 摘要
基于学习和物理信息的方法越来越多地用于受控非线性动力系统的逆估计。然而，在许多此类方法中，使未知输入重构有意义的理论要求，即哈达玛意义上的适定性，常常被忽视，或通过没有明确保证的通用正则化项来弱化处理。在本文中，我们采用一种互补的观点，即这些控制理论和结构条件指导估计器设计并约束其训练。因此，我们开发了一种物理信息的输入-状态神经估计器，用于具有部分测量的非线性受控系统中的联合未知输入和状态估计。在目前的工作中，这个通用框架实例化于一个自主心脏调节模型，提供了一个具体的研究案例。该估计器被表述为依赖于时间和测量输出的逆神经映射，并在数据保真度和动态一致性约束下进行训练。为了确保其符合鲁棒估计中施加的相同结构要求，我们通过微分代数消元推导出左可逆性条件，并将得到的约束直接嵌入到训练目标中。我们进一步先验分析了逆映射对输出扰动的稳定性，并推导了一个保守的Lipschitz界，用于指导代价函数超参数的调整。该框架在模拟数据（其中真实数据可用）和两个不同的真实心血管记录数据集上进行了评估。结果表明，将控制理论的可解性约束纳入物理信息学习，比仅具有前向一致性更能提高逆推理的可靠性。

## Abstract
Learning-based and physics-informed methods are increasingly used for inverse estimation in controlled nonlinear dynamical systems. However, in many such approaches, the theoretic requirements that make unknown-input reconstruction meaningful, namely well-posedness in the sense of Hadamard, are often disregarded or weakly addressed through generic regularization terms with no explicit guarantees. In this work, we adopt a complementary viewpoint in which these control-theoretic and structural conditions inform the estimator design and constrain its training. We thus develop a physics-informed input-state neural estimator for joint unknown-input and state estimation in nonlinear controlled systems with partial measurements. In the present work, this general framework is instantiated on a model of autonomic cardiac regulation, provides a concrete study case. The estimator is formulated as an inverse neural map conditioned on time and measured outputs, and is trained under data fidelity and dynamical consistency constraints. To ensure it complies with the same structural requirements imposed in robust estimation, we derive left-invertibility conditions by differential-algebraic elimination and embed the resulting constraints directly into the training objective. We further analyze a priori the stability of the inverse mapping to output perturbations and derive a conservative Lipschitz bound that guides the tuning of cost functional hyper-parameters. The framework is evaluated on simulated data, where ground truth data is available, and on two distinct datasets of real cardiovascular recordings. The results show that incorporating control-theoretic solvability constraints into physics-informed learning improves the reliability of inverse inference beyond forward consistency alone.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：现有基于学习的逆向估计方法（如物理信息神经网络 PINNs）在受控非线性动力系统中进行未知输入和状态估计时，普遍忽视 **Hadamard 适定性**（唯一性、稳定性和存在性）要求。这些方法通常仅通过通用正则化项（如动力学残差）弱化不适定性，但缺乏显式保证，导致估计出的输入虽能拟合观测数据却可能非唯一、不鲁棒。
- **整体含义**：本文提出应将控制理论中的可解性结构性条件（如左可逆性、输出-输入稳定性）直接嵌入学习估计器的设计和训练过程，而非仅作为后处理或启发式约束。这为科学机器学习（SciML）中的逆推理提供了理论驱动的新范式，尤其适用于生物医学中未知调控信号（如自主神经指令）的重建。

### 论文提出的方法论

- **核心思想**：构建一个 **物理信息输入–状态神经估计器（PIISNE）**，其网络输出为估计的状态（心率 \( \hat{H} \)、血压 \( \hat{P} \)）和未知输入（压力设定点 \( \hat{P}_{\text{sp}} \)），通过多目标损失联合学习，其中最关键的是**将微分代数消元导出的左可逆性条件作为显式约束项**。
- **关键技术细节**：
    1. **左可逆性分析**：针对自主心脏调节模型（非线性、输入仅通过非线性 sigmoid 函数作用），利用 Rosenfeld–Gröbner 算法消去状态，得到输入-输出微分代数方程，并推导出唯一重构需满足的条件（如非退化分母 \( D(s,Y) \neq 0 \)、sigmoid 非饱和 \( s\notin\{0,1\} \) 等）。
    2. **损失函数**：由三部分构成
        - \( \mathcal{L}_{\text{data}} \)：预测心率与测量值的均方误差。
        - \( \mathcal{L}_{\text{dyn}} \)：动力学残差（ODE 约束），但计算总导数时需同时考虑时间自微分和通过 \( h_{\text{norm}} \) 的隐式依赖（使用数值差分计算 \( dh_{\text{norm}}/dt \)）。
        - \( \mathcal{L}_{\text{ID}} \)：左可逆性约束，包括：
            - \( \mathcal{L}_{\text{INQ}} \)：确保中间变量 \( Y \) 在 \([-V_H, \beta_H]\) 内。
            - \( \mathcal{L}_{\text{sat}} \)：避免 sigmoid 饱和，使 \( s \in [\varepsilon_{\text{sat}}, 1-\varepsilon_{\text{sat}}] \)。
            - \( \mathcal{L}_{\text{degen}} \)：强制 \( |D(s,Y)| \geq \varepsilon_{\text{deg}} \)。
    3. **稳定性分析**：推导逆映射的 Lipschitz 常数 \( C_H \)，其大小由边际超参数 \( \varepsilon_{\text{sat}} \)、\( \varepsilon_{\text{deg}} \) 和模型参数决定。通过数值模拟划定稳定区域，指导超参数选择，使输出误差在逆映射中不被过度放大。
    4. **网络结构**：7 层全连接 MLP（[2,10,25,40,20,10,3]，GELU 激活），输入为归一化时间 \( t_{\text{norm}} \) 和归一化心率 \( h_{\text{norm}}(t) \)。

### 实验设计

- **数据集**：
    - **模拟数据**：500 条合成 \( P_{\text{sp}} \) 曲线（bump 形状），通过 ODE 生成对应的 \( H \) 和 \( P \)，提供完整真值。
    - **PASS 数据集**：48 名参与者的心率时间序列（从 ECG 提取），仅含 \( H \)。
    - **3CI 数据集**：30 名参与者的心率与连续血压记录（Finometer），含 \( H \) 和 \( P \)。
- **基准方法**：
    - **滑动模观测器（SMO）**：将 \( P_{\text{sp}} \) 视为慢变增广状态，采用模型基估计器进行对比。
- **对比与消融**：
    - **跨被试泛化**：用一个参与者（source key）训练的网络在其余参与者（target keys）上测试，绘制 NRMSE 热力图和 violin 图。
    - **消融实验**：移除左可逆性损失项（\( \mathcal{L}_{\text{ID}} \)），观察自预测（在同一参与者内部）上的失败案例。
    - **稳定性常数数值模拟**：在网格 \( \varepsilon_{\text{sat}} \in [10^{-3},0.49] \)、\( \varepsilon_{\text{deg}} \in [10^{-3},10] \) 上计算 \( \log_{10} C_H \)，并进行蒙特卡罗不确定性分析。

### 资源与算力

- **明确提及**：实验在 **Python 3.14.2**，配备 **32GB RAM，Intel Core i7-13800H CPU，NVIDIA RTX Ada GPU** 的 PC 上进行。
- **训练细节**：使用 PyTorch，Adam 优化器（学习率 0.1，batch size 1500），最多 95 个 epoch，早停（20 epoch 无改善则停止）。**未说明单次训练具体耗时或 GPU 数量**，推测为单 GPU 实验。

### 实验数量与充分性

- **实验数量**：
    - **模拟数据**：500 个合成轨迹训练与测试。
    - **PASS 数据**：48 个参与者，每个参与者 7500 样本训练，剩余做独立测试；跨被试热力图涉及 48×48 组合。
    - **3CI 数据**：30 名被试测试。
    - **消融实验**：展示了 3 个典型失败案例（被试 2115、2117、2121）。
    - **SMO 对比**：3 个典型失败案例。
    - **稳定性常数扫描**：300×300 网格点计算。
- **充分性评价**：
    - **优点**：覆盖模拟与真实多模态数据，跨被试泛化评估充分，消融实验直接证明了左可逆性约束的必要性。
    - **不足**：对比方法仅 SMO 一种，缺少与现代学习方法（如扩展卡尔曼滤波、高斯过程潜变量模型、其他 PINN 变体）的系统比较。此外，消融实验仅展示定性失败案例，未提供定量 NRMSE 变化。

### 论文的主要结论与发现

1. **理论驱动有效**：将控制理论中的左可逆性条件显式嵌入学习目标，显著提升了未知输入和状态估计的可靠性和唯一性，仅靠前向一致性无法保证。
2. **稳定性可控**：Lipschitz 常数分析表明，选择合适的边际超参数 \( \varepsilon_{\text{deg}} \)（非退化）和 \( \varepsilon_{\text{sat}} \)（非饱和）可控制逆映射的条件数，指导超参数选择。
3. **跨被试泛化**：PIISNE 在 PASS 数据集上跨被试泛化表现良好（部分 source key 的 NRMSE 分布窄且低），并能迁移至 3CI 和模拟数据集，证明其学习到的不是单纯数据拟合。
4. **限制条件**：固定生理参数（如每搏输出量）导致部分跨被试泛化失败（如 target 2142 与 source 2141 不兼容）；SMO 虽能拟合心率但产生非生理的 \( P_{\text{sp}} \) 振荡，凸显输入动态假设的局限性。

### 优点

- **理论深度**：首次系统地将 Hadamard 适定性与微分代数消元引入 PINN 训练，提供了解的唯一性保证，而非仅依赖正则化。
- **稳定性量化**：推导显式 Lipschitz 界并数值分析，为超参数 \( \varepsilon_{\text{sat}} \)、\( \varepsilon_{\text{deg}} \) 的选取提供了理论依据。
- **跨模泛化验证**：在三个数据集（含真实数据）上评估，并分析了泛化成功与失败的模式，揭示了参数敏感性。
- **消融实验说服力**：清晰展示移除左可逆性约束后即使自预测也出现严重估计失败，验证了约束的关键作用。

### 不足与局限

- **对比基准不足**：仅与滑动模观测器（SMO）对比，缺乏与其他神经网络逆估计方法（如基于流的可逆网络、条件变分自编码器、标准 PINN）的实验比较，难以全面评估方法优势。
- **模型限制**：当前模型未考虑延迟机制（如生理学中的 Mayer 波），作者承认这是未来工作；固定所有生理参数（尤其每搏输出量）导致部分泛化失败，缺乏参数自适应机制。
- **数值微分依赖**：为计算 \( dh_{\text{norm}}/dt \) 使用有限差分，可能放大测量噪声，论文虽提及平滑但未深入讨论噪声鲁棒性。
- **定量消融指标缺失**：消融实验仅展示定性图例，未提供诸如 NRMSE 或 SMAPE 的统计变化，说服力有限。
- **资源与可复现性**：未提供代码或预训练模型；训练时间未报告，单次训练的可重复性不明确。
- **应用范围**：主要针对一维输入和两维状态的特殊模型，方法推广到高维或不同结构非线性系统的可行性尚需进一步验证。

（完）
