---
title: Conservation Laws from Data Symmetry in Neural Networks
title_zh: 神经网络中数据对称性导致的守恒定律
authors: "Jakob Galley, Vahid Shahverdi, Axel Flinth"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10913v1"
tags: ["query:geo-phys-ai"]
score: 8.0
evidence: 探索神经网络中数据对称性导致的守恒定律，联系等变性与训练过程中的守恒量
tldr: 在神经网络梯度流训练中，数据的内在对称性是否导致守恒量的产生取决于损失函数类型。对于解析非多项式损失，数据对称性一般不会诱导额外的积分运动；而对于均方误差（MSE）损失，数据增强在某些情况下可以产生额外的守恒量。为了系统描述这一现象，论文提出了“张量化网络”（tensorizable networks）框架，这类架构通过中间表示将参数和输入分离，涵盖线性网络、多项式网络和闪电注意力等。该工作揭示了数据对称性与训练动力学守恒量之间的复杂关系，对理解神经网络训练特性具有重要意义。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10913v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 845, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10913v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 853, \"height\": 644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10913v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 851, \"height\": 644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10913v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1020, \"height\": 1021, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10913v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1425, \"height\": 427, \"label\": \"Figure\"}]"
motivation: 探究训练数据的固有对称性是否在神经网络梯度流训练中产生守恒量，以理解数据增强对训练动力学的影响。
method: 假设损失函数解析且非多项式，证明数据对称性一般不产生额外守恒量；对于MSE损失，分析数据增强效应，并提出张量化网络框架描述此现象。
result: 解析非多项式损失下数据对称性不诱导额外积分；MSE损失下数据增强可能产生额外守恒量；张量化网络框架捕获了该机制。
conclusion: 数据对称性是否产生守恒量依赖于损失函数类型，张量化网络为理解该现象提供了数学框架。
---

## 摘要
我们探讨训练数据的内在对称性是否会在神经网络的梯度流训练过程中导致守恒量。假设损失函数是解析且非多项式的，我们证明数据对称性通常不会诱导任何额外的运动积分。另一方面，对于均方误差损失，存在数据增强产生额外守恒量的情况。我们利用可张量化网络构建了一个框架来描述这一现象。可张量化网络是一类架构，其参数和输入的依赖性可以通过中间表示分离，包括线性网络、多项式网络以及闪电注意力。

## Abstract
We explore whether intrinsic symmetries of the training data lead to conserved quantities during gradient-flow training of neural networks. Under the assumption that the loss function is analytic and non-polynomial, we prove that data symmetries generically do not induce any additional integrals of motion. For mean squared error (MSE) loss, on the other hand, there are situations in which data augmentation yields extra conserved quantities. We build a framework, utilizing \emph{tensorizable networks} to describe this phenomenon. Tensorizable networks are a family of architectures whose dependence on parameters and inputs can be separated using an intermediate representation. They include linear and polynomial networks, as well as Lightning Attention.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：探究训练数据的内在对称性（如置换、旋转等群作用）是否能在神经网络的梯度流训练过程中产生新的守恒量（积分运动）。
- **研究动机**：守恒定律能约束优化路径，帮助理解初始依赖性、收敛性和隐式偏置。以往研究多关注与数据无关的守恒量，而实际数据常带有对称性（如通过数据增强引入）。作者希望厘清数据对称性与守恒定律之间的因果关系。
- **整体含义**：揭示了数据对称性是否产生守恒量强烈依赖于损失函数的类型，为理解数据增强对训练动力学的影响提供了理论基础，并提出了“可张量化网络”这一统一框架来描述 MSE 损失下的情况。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：
  - 对于解析非多项式损失（如 logistic、exponential loss），数据对称性一般不会产生新的守恒量；对于多项式损失（如 MSE），情况不同，数据增强可能产生新的守恒量。
  - 通过分析梯度流中梯度张成的子空间是否因数据对称性而缩小来判断是否存在新守恒量。
- **关键技术细节**：
  - **非多项式损失情形**：假设损失函数 \( \ell(\hat{y}, y) = L(\langle \hat{y}, y \rangle) \)，其中 \( L \) 解析且非多项式。利用泰勒展开幂级数具有无穷多非零项，结合 Assumption 1（参数非退化条件），证明群平均后的梯度张成空间与原梯度张成空间相等（Theorem 3），因此无新守恒量。
  - **MSE 损失情形**：引入**可张量化网络**定义：存在特征空间 \( H \)、输入映射 \( T(x) \) 和参数映射 \( M(\theta) \)，使得 \( f_\theta(x) = M(\theta) T(x) \)。假设 \( T \) 与数据对称群 \( G \) 兼容（\( T(\rho_X(g)x) = \rho_H(g) T(x) \)），则增强后的 MSE 损失 \( J_\pi^G \) 在 \( H \) 上的二次型和线性型成为 \( G \)-等变。定义群 \( H \) 为保持所有 \( G \)-等变对称算子和线性映射的变换。若参数化在某个参数块 \( P \) 上实现了 \( O(V) \)-对称性（即存在 \( B_a \in H \) 使得 \( M(aP,\xi) = M(P,\xi) B_a \)），则增强损失对左乘正交群不变。由此证明 \( P \) 的列空间在梯度流下不变（Theorem 4），从而 \( P^\top P \) 的 Gram 矩阵或列空间投影为守恒量。
- **公式与算法流程**（文字说明）：
  - 梯度流：\( \dot{\theta} = -\nabla E_\pi(\theta) \)。
  - 群增强风险：\( E^G_\pi(\theta) = \mathbb{E}_{(x,y)\sim\pi, g\sim\mu} \ell(f_\theta(\rho_X(g)x), \rho_Y(g)y) \)。
  - 守恒量条件：\( \langle \nabla I(\theta), \nabla E^G_\pi(\theta) \rangle = 0 \) 对所有 \( \pi \) 成立。
  - 关键推导：利用非多项式损失泰勒级数无穷项构造 Vandermonde 矩阵可逆性；利用 MSE 损失多项式性质及第一基本定理证明列空间守恒。

#### 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法
- **实验场景**：
  1. **线性模型**（输入 \( \mathbb{R}^3 \)，输出 \( \mathbb{R} \)），数据具有 \( C_3 \) 循环置换对称性。对比标准 MSE 损失与增强 MSE 损失（每个点用完整 \( C_3 \) 轨道增强）。
  2. **单头闪电注意力模型**（无归一化），输入维度 \( d=8 \)，token 数 \( n=5 \)，特征维度 \( r=m=1 \)，数据具有右乘 \( O(d) \) 对称性。标签由随机教师模型生成。对比标准 MSE 与增强 MSE（用 4096 个 Haar 采样正交矩阵近似群平均）。
- **基准（benchmark）**：论文未提供标准基准数据集，而是针对特定对称性进行理论验证性实验。
- **对比方法**：仅对比有无数据增强（即标准损失 vs. 群增强损失）下的梯度下降轨迹，跟踪守恒量的保持程度。

#### 4. 资源与算力
- **未明确说明**：论文未提及使用的 GPU 型号、数量、训练时长等算力信息。实验规模极小（线性模型 4 个点，闪电注意力 10 个基础输入），推测可在普通 CPU 上完成。

#### 5. 实验数量与充分性
- **实验数量**：仅两组实验（线性模型 + 闪电注意力），每组展示不同初始化下的轨迹。
- **充分性评估**：
  - **线性模型**：展示了 6 次训练运行的偏差图，增强后守恒量保持到数值精度，非增强则显著变化。实验直观验证了理论。
  - **闪电注意力**：展示了 6 次初始化的列空间距离，增强后远小于非增强。
  - **不足**：实验规模小，仅验证单参数设置；未进行超参数扫描、不同群大小、不同网络深度或宽度的消融；未对比其他损失函数；仅在 MSE 损失下验证了定理 4，未对非多项式损失部分做实验验证（但理论已证明）。实验客观但不够全面。

#### 6. 论文的主要结论与发现
- **结论 1**：对于解析非多项式边距损失（如 logistic 或 exponential loss）和有限对称群，在非退化区域（Assumption 1）内，数据对称性不会产生任何新的守恒量（Theorem 3）。
- **结论 2**：对于 MSE 损失，数据对称性通过可张量化网络中的“提升对称性”可能创造新的守恒量。具体地，若参数块 \( P \) 在提升空间上实现了正交群对称性，则 \( P \) 的列空间在增强梯度流下保持不变（Theorem 4）。
- **发现**：数据对称性产生守恒量的能力取决于损失函数的多项式特性；甚至离散数据对称性（如有限群）也可在参数空间诱导连续对称性（如 \( O(V) \) 或 \( U(V) \)），从而产生守恒量。

#### 7. 优点：方法或实验设计上的亮点
- **理论深度**：建立了与 Noether 定理的类比，将数据对称性转化为参数空间的新对称性，并严格区分了不同损失函数下的行为。
- **框架创新**：提出“可张量化网络”概念，统一了线性、多项式、闪电注意力等模型，为后续研究提供了数学工具。
- **证明严谨**：对非多项式损失使用了泰勒级数和 Vandermonde 矩阵的可逆性论证，对 MSE 损失利用了第一基本定理和 ODE 理论。
- **实验直观**：通过简单模型验证了抽象理论，用偏差图和列空间距离直观展示了守恒量的保持。

#### 8. 不足与局限
- **理论假设**：
  - 非多项式损失结果要求损失解析、群有限、参数处于非退化开集（Assumption 1），实际中可能不满足（如 ReLU 非解析）。
  - MSE 损失结果要求网络可张量化，非所有架构（如带非线性激活的深度网络）都满足；且要求提升映射 \( T \) 与群作用兼容。
  - 群增强风险的数据独立性假设（\( \pi \) 任意）可能弱于实际中仅依赖训练集。
- **实验覆盖**：
  - 缺少对非多项式损失情形的实验验证（如 logistic loss 是否确实无新守恒量）。
  - 仅测试了 MSE 损失下的两个简单模型，未在更复杂架构（如 ResNet、Transformer）上验证。
  - 未探讨当数据对称性不完美（近似对称）或群无限（如连续群）时的鲁棒性。
- **应用限制**：文中守恒量（如列空间）对实际训练优化性能的直接影响未明确；未讨论如何在实践中利用这些守恒量改进训练（如加速收敛、指导初始化）。
- **计算消耗**：闪电注意力实验中用 4096 个 Haar 采样近似连续群平均，计算成本较高，实际中可能不可行。

（完）
