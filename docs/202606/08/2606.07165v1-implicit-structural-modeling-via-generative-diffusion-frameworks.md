---
title: Implicit Structural Modeling via Generative Diffusion Frameworks
title_zh: 基于生成扩散框架的隐式结构建模
authors: "Yimin Dou, Xinming Wu, Zhixiang Guo, Hui Gao, Buyu Deng"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07165v1"
tags: ["query:geo-phys-ai"]
score: 9.0
evidence: 基于扩散的隐式结构建模，用于地下地质构型
tldr: 隐式结构建模面临复杂断层场景下拓扑一致性和运动学合理性不足的挑战。提出基于扩散模型的方法，通过仿真合成数据设计条件注入编码器，并利用预训练骨干网络进行条件训练，使模型更稳定地传播结构约束。实验表明，模型能有效处理走滑断层、花状断层等复杂类型，甚至在逆冲推覆的非单调标量场中也能生成可靠模型。该方法展现了扩散模型在隐式结构建模中的显著潜力与泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07165v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1859, \"height\": 938, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07165v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1721, \"height\": 707, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07165v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1836, \"height\": 781, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07165v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1803, \"height\": 950, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07165v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1804, \"height\": 1142, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07165v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1757, \"height\": 1694, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07165v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1771, \"height\": 1257, \"label\": \"Figure\"}]"
motivation: 现有方法在复杂断层（如相交、分支、逆冲推覆）中难以保持拓扑一致性与运动学合理性。
method: 构建合成数据，设计条件注入编码器，利用预训练骨干网络进行扩散模型条件训练。
result: 模型有效处理走滑、花状等复杂断层，并在逆冲推覆中生成可靠隐式模型。
conclusion: 扩散模型框架提升了隐式结构建模在复杂地质场景下的鲁棒性与泛化性。
---

## 摘要
隐式结构建模能够支持理解地下空间配置、揭示地质演化模式以及实现地质过程的定量模拟，从而具有重要的科学和工程价值。传统方法将其表述为优化问题或框架插值以拟合连续标量场，而机器学习方法通常采用判别式回归直接预测隐式模型。然而，在涉及断层交汇、分支和逆冲推覆体的复杂场景中，这些方法仍难以保持拓扑一致性和运动学合理性。在这项工作中，我们开发了一种基于扩散模型的隐式结构建模方法。我们通过基于模拟的合成流程构建训练数据集，并设计专用的条件注入编码器，使得条件分支能够快速收敛，同时在整个扩散过程中有效强化输入条件先验，从而更稳定地传播结构约束。然后，我们将这些条件特征注入到预训练于大规模自然图像上的骨干网络中，以实现扩散模型的条件训练。尽管我们的合成数据仅包含相对风格化的正断层系统，但实验表明模型具有很强的泛化能力，能够有效处理诸如走滑断层和复杂花状断层系统等多种复杂结构类型。更重要的是，即使在具有挑战性的逆冲推覆环境中（标量场变得非单调且出现急剧深度不连续性），模型仍能生成可靠的隐式结构模型。

## Abstract
Implicit structural modeling can support understanding subsurface spatial configurations, revealing patterns of geological evolution, and enabling quantitative simulation of geological processes, thereby offering substantial scientific and engineering value. Conventional approaches formulate it as an optimization problem or framework interpolation to fit a continuous scalar field, whereas machine learning methods typically adopt discriminative regression to directly predict implicit models. However, in complex scenarios involving fault intersections, branching, and thrust nappes, these methods still struggle to maintain topological consistency and kinematic plausibility. In this work, we develop an implicit structural modeling approach based on diffusion models. We construct a set of training data through a simulation based synthesis pipeline and design a dedicated encoder for conditional injection, allowing the conditional branch to converge rapidly while effectively reinforcing the input conditional priors throughout the diffusion process, thereby more stably propagating structural constraints. We then inject these conditional features into a backbone network pretrained on large scale natural images to enable conditional training of the diffusion model. Although our synthetic data include only a relatively stylized normal fault system, experiments demonstrate strong generalization, enabling the model to effectively handle diverse complex structural types such as strike slip faults and intricate flower fault systems. More importantly, even in challenging thrust nappe settings where the scalar field becomes non monotonic and exhibits abrupt depth discontinuities, the model can still generate reliable implicit structural models.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：隐式结构建模旨在通过连续标量场（如相对地质时间RGT场）统一描述地下地质构型，支撑地层框架构建、圈闭识别、储层建模等工程任务，以及结构演化、沉积系统重建等科学研究。
- **现有挑战**：传统方法（DSI、PFM）依赖数学光滑性约束，难以处理强非线性、不连续性和局部突变；基于判别式回归的深度学习方法（如DeepISMNet）只能学习训练分布的均值和趋势，缺乏对非唯一性和不确定性的表示能力，且在复杂断层（相交、分支、逆冲推覆）中难以保持拓扑一致性和运动学合理性。
- **核心目标**：提出一种基于扩散模型的隐式结构建模方法，利用生成模型的多样性和不确定性表达能力，提升对复杂地质结构的泛化能力和建模质量。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用条件扩散模型，将隐式结构建模视为一种条件生成任务。输入条件包括稀疏的断层骨架（fault labels）和水平约束（sparse horizon constraints），输出完整的相对地质时间（RGT）标量场。通过构建专用的条件注入机制，使模型能够稳定传播结构约束，并利用预训练于大规模自然图像的骨干网络（Stable Diffusion UNet）保持强大的生成先验。
- **关键技术细节**：
  - **数据准备**：通过仿真合成流程生成4000对二维合成样本（RGT场+断层标注），训练时从RGT场中提取稀疏水平和断层标签作为条件输入，完整RGT场作为监督信号。
  - **扩散框架**：采用Stable Diffusion范式，在VAE压缩8倍的潜空间中进行扩散。前向过程逐步加噪，逆向过程在条件引导下迭代去噪。训练目标为噪声预测误差（公式4）。
  - **条件编码器**：设计专用条件注入编码器，采用MaxPooling替代步长卷积以保留极值和边界响应，并使用残差结构防止条件信号逐层衰减。该编码器将像素空间的条件信息映射到与潜变量对齐的潜空间特征。
  - **双分支条件注入**：由于断层（高频骨架约束）和水平（低频数值约束）特征差异大，分别构建两个独立的条件分支，在UNet解码器的不同尺度上通过零初始化的卷积注入残差特征。注入方式为：`˜d(k) = d(k) + Z_k(s_k, f_c^{(k)})`，其中Z为零初始化卷积，s_k为可学习的缩放系数。双分支设计避免了条件竞争，确保两类约束均被稳定坚持。
  - **训练策略**：冻结UNet编码器以保留通用先验，仅训练条件分支和UNet解码器，以适应地球科学领域。采用DDPM训练、DDIM确定性采样。优化器AdamW，学习率1e-5，batch size 50，总步数200k，并使用指数移动平均（EMA）。

## 3. 实验设计：数据集、场景、基准方法

- **训练数据**：4000对二维合成样本，以正断层系统为主，风格化、模拟生成。
- **测试场景**：
  1. **合成数据测试**：随机生成的合成样本（正断层系统），包括训练分布内和分布外（如陡倾断层）情况。
  2. **走滑/花状断层系统**：手工绘制的三种花状结构（负花、正花、混合花），测试分布外泛化。
  3. **逆冲/推覆系统**：两个简化俯冲-逆冲例子，评估非单调标量场和深度跳跃下的鲁棒性。
  4. **真实数据测试**：
     - 三个走滑花状结构真实地震剖面（来自Huang et al.，2017）。
     - 三个逆冲-推覆系统真实案例（来自Butler et al.，2020）：包括深海褶皱-逆冲带、理想化褶皱-逆冲系统、薄皮逆冲系统。
- **对比方法**：
  - SD-UNet-Reg：判别式回归框架（类似Bi et al.），使用相同UNet骨干但去除扩散时间嵌入。
  - SD-UNet-GAN：生成式GAN框架，同样骨干。
  - 本文方法：基于扩散模型的双分支条件注入方法。
- **评估方式**：由于隐式建模的非唯一性，主要采用定性比较（视觉质量、拓扑一致性、约束遵守情况），未采用MSE/SSIM等像素级指标。

## 4. 资源与算力

- 文中明确说明：训练在**两块NVIDIA H20 GPU**上完成。
- 训练步数：200,000步，batch size=50。
- 其他算力细节（如单卡训练时间、总时长）未给出。

## 5. 实验数量与充分性

- **实验组数**：共包含3大类合成数据测试（正断层、花状、逆冲）和3个真实数据案例（走滑、逆冲推覆3个场景），以及额外的数据飞轮示例。但未做严格的消融实验（如去掉双分支、替换条件编码器等），也未提供定量指标对比。
- **充分性评价**：实验设计较全面，覆盖了多种复杂结构类型（正断层、花状、逆冲推覆），且包含真实数据验证，展示了强大的泛化能力。但缺乏消融实验和定量指标，导致无法严格验证每个组件（如MaxPooling、残差、双分支）的贡献；对比方法仅涉及判别回归和GAN，未与传统方法（DSI、PFM）或最新生成模型（如其他扩散变体）对比。实验公平性方面，所有方法采用相同骨干，具有可比性。整体而言，实验在定性层面充分，但在定量和消融层面有所不足。

## 6. 论文的主要结论与发现

- 扩散模型方法在隐式结构建模中显著优于判别式回归和GAN框架，能更稳定地满足断层和水平约束，减少伪影和局部畸变，更好保持地层连续性和全局拓扑一致性。
- 模型仅用正断层合成数据训练，却能有效泛化到走滑、花状、逆冲推覆等未见过的复杂结构，甚至能处理非单调标量场（逆冲推覆中的深度跳跃），体现了强大的分布外泛化能力。
- 条件编码器和双分支注入设计有效分离了断层（高频骨架）和水平（低频数值）约束，避免了条件竞争，实现了更稳健的生成控制。
- 基于该方法的“数据飞轮”概念：生成高质量隐式结构后，结合属性赋值和正演模拟，可合成多种模态的地球物理数据（如地震、阻抗、伽马射线），为更广泛的地学AI任务提供训练数据。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - 双分支条件注入：针对断层和水平约束的不同特征分别建模，避免特征冲突，提升约束稳定性。
  - 条件编码器的设计（MaxPooling+残差）有助于保留结构边界和极值，防止深层衰减。
  - 冻结UNet编码器、仅训练解码器和条件分支，有效利用预训练先验，降低领域迁移难度。
  - 利用仿真合成数据训练却能泛化到真实复杂数据，证明方法的强泛化能力。
- **实验亮点**：
  - 测试场景丰富，涵盖多种断层类型（正断层、走滑花状、逆冲推覆）和真实数据。
  - 定性对比展示了清晰的视觉差异，尤其是在花状结构和逆冲推覆中，本文方法明显优于基线。
  - 提出“数据飞轮”概念，展示了模型在数据生成方面的应用潜力，具有实际工程价值。

## 8. 不足与局限

- **实验覆盖不足**：
  - 缺乏消融实验：未验证每个设计组件（如MaxPooling、残差、双分支）的独立贡献。
  - 缺乏定量指标：由于非唯一性，虽可理解，但仍有改进空间（如使用地质一致性得分、接触关系保持率等）。
  - 对比方法较少：仅与判别回归和GAN对比，未与传统隐式建模方法（DSI、PFM）或最新扩散变体（如RePaint、ControlNet标准形式）对比。
- **偏差风险**：
  - 训练数据仅为风格化正断层系统，可能隐含对正断层几何的偏好，尽管泛化能力好，但仍可能在某些复杂结构（如压扭性断层）上存在偏差。
  - 定性比较主观性强，缺乏统计学显著性检验。
- **应用限制**：
  - 当前仅限于2D剖面建模，三维扩展尚未验证（论文也暗示了未来方向）。
  - 合成数据与真实数据之间仍存在域差异，真实数据中的噪声和不确定性未被明确建模。
  - 未讨论模型对输入条件稀疏度和质量的敏感性，实际应用中水平约束不够密集时表现未知。
- **可重复性**：未提供开源代码或详细超参数设置，复现存在困难。

（完）
