---
title: Adaptive Hard-Soft Physics-Informed Neural Networks for Robust Boundary-Constrained PDE Solving
title_zh: 自适应硬-软物理信息神经网络用于鲁棒的边界约束偏微分方程求解
authors: "Duc Tien Nguyen, Trinh Minh Tuan, Nguyen Duc Manh, Vu Linh Nguyen, Dinh Gia Ninh"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23359v1"
tags: ["query:geo-phys-ai"]
score: 9.0
evidence: 硬-软物理信息神经网络，通过构造强制边界约束，超越损失惩罚方式
tldr: 针对传统PINN将边界条件作为软约束导致收敛慢、精度低且需手动调参的问题，提出HSPINN框架。通过解析或多项式提升、掩码函数等精确施加Dirichlet和周期边界条件（硬约束），而PDE残差等作为软约束，并用逆共享softmax自适应平衡损失权重。在Poisson、Burgers和对流问题中，HSPINN比传统PINN收敛更快、精度更高、稳定性更强，为物理约束深度学习提供了通用可扩展方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23359v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1558, \"height\": 726, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1563, \"height\": 1026, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1621, \"height\": 1313, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1596, \"height\": 1061, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1625, \"height\": 1303, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1599, \"height\": 1050, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23359v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1622, \"height\": 1310, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23359v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1661, \"height\": 327, \"label\": \"Table\"}]"
motivation: 传统PINN中边界软约束导致优化条件差、收敛慢、精度低，且损失权重需手动调参。
method: HSPINN通过解析或多项式提升、掩码函数和周期特征映射精确强制边界条件，其余约束作为软损失，并用逆共享softmax自适应平衡损失权重。
result: 在Poisson、Burgers和对流问题中，HSPINN收敛更快，精度更高，且稳定性优于传统PINN。
conclusion: HSPINN有效解决了边界强制和损失平衡问题，为科学工程中的PDE求解提供了通用鲁棒的基础框架。
---

## 摘要
物理信息神经网络（PINNs）通过将物理原理嵌入学习过程，为求解偏微分方程（PDEs）提供了一种有效方法。然而，传统PINN公式将所有约束作为复合损失中的软惩罚项，常因优化景观条件不良而导致收敛缓慢、对损失权重缩放敏感以及边界强制执行不准确。为克服这些局限，本研究提出了一种具有自适应损失权重的统一硬-软物理信息神经网络（HSPINN）。在该框架中，通过解析或多项式提升、掩码函数和周期特征映射，精确构造狄利克雷和周期边界条件，而控制方程残差、诺伊曼通量和初始条件则作为软约束处理。一种逆份额softmax策略在训练过程中动态平衡各损失分量的相对重要性，消除了手动惩罚调整，提高了梯度稳定性。该公式确保了整个优化过程中的边界可接受性，并增强了收敛效率和数值鲁棒性。应用于代表性椭圆型（泊松）、抛物型（伯格斯）和双曲型（周期边界对流）问题，表明HSPINN相比传统PINN始终实现更快的收敛、更高的精度和更强的稳定性，为科技领域中的物理约束深度学习建立了通用且可扩展的基础。

## Abstract
Physics-informed neural networks (PINNs) provide an effective way to solve partial differential equations (PDEs) by embedding physical principles into the learning process. However, the conventional PINN formulation, in which all constraints are imposed as soft penalty terms within a composite loss, often exhibits slow convergence, sensitivity to loss weight scaling, and inaccurate boundary enforcement due to poor conditioning of the optimization landscape. To address these limitations, this study proposes a unified hard--soft physics--informed neural network (HSPINN) with adaptive loss weighting. In this framework, Dirichlet and periodic boundary conditions are enforced exactly by construction through analytical or polynomial lifting, masking functions, and periodic feature mappings, while the governing PDE residuals, Neumann fluxes, and initial conditions are treated as soft constraints. An inverse-share softmax strategy dynamically balances the relative importance of individual loss components during training, eliminating manual penalty tuning and improving gradient stability. This formulation ensures boundary admissibility throughout optimization and enhances convergence efficiency and numerical robustness. Applications to representative elliptic (Poisson), parabolic (Burgers), and hyperbolic (convection with periodic boundaries) problems demonstrate that HSPINN consistently achieves faster convergence, higher accuracy, and greater stability than conventional PINNs, establishing a general and scalable foundation for physics-constrained deep learning across science and technology.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：传统物理信息神经网络（PINNs）将偏微分方程（PDE）求解转化为复合损失函数的最小化问题，其中所有约束（包括边界条件、初始条件、PDE残差）都被当作软惩罚项。这种全软约束方式存在三大缺陷：
  - **收敛缓慢**：损失项之间量级差异大，优化景观病态，导致梯度不稳定。
  - **边界执行不准确**：软惩罚无法严格满足边界条件，尤其在Dirichlet边界上，网络输出与指定值存在偏差。
  - **手动调参敏感**：需要在不同损失分量前人为设定权重（λ），费时且易导致次优结果。
- **整体含义**：该论文旨在通过**硬–软约束混合**的策略，从根本上改善PINNs的边界强制能力和训练鲁棒性，为科学工程中各类PDE求解提供一个通用、可扩展的神经网络框架。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将**已知且易构造的约束**（如Dirichlet边界、周期边界）通过**网络架构直接强制**（硬约束），而将**内部物理规律**（PDE残差）、Neumann通量、初始条件等作为**软约束**通过损失最小化实现。通过这种方式，搜索空间被限制在边界相容的函数族内，优化景观更平滑。
- **关键技术细节**：
  - **硬约束构造**：
    - **Dirichlet边界**：通过**解析/多项式提升函数** \(h_D(x,t)\) 和**掩码函数** \(m_D(x)\) 构造网络输出：
      \[
      u_\theta(x,t) = h_D(x,t) + m_D(x) \, v_\theta(x,t)
      \]
      其中 \(h_D\) 在边界处等于给定值 \(g_D\)，\(m_D\) 在边界处为零，内部为正。这样对于任意参数 \(\theta\)，\(u_\theta\) 自动满足Dirichlet条件。
    - **周期边界**：对空间坐标进行**傅里叶特征映射**（例如 \(\phi(x,t)=[\sin(2\pi x/L), \cos(2\pi x/L), t]\)），使网络输出天然具有周期性质，无需额外惩罚。
  - **软约束**：
    - PDE残差 \(R_{\text{int}} = \mathcal{N}[u_\theta] - f\)
    - Neumann边界残差 \(R_N = \mathbf{n}\cdot\nabla u_\theta - g_N\)
    - 初始条件残差 \(R_{ic} = u_\theta(x,0) - u_0(x)\)
  - **自适应损失权重**：采用**逆-份额softmax**（inverse-share softmax）策略，基于各损失项的梯度范数自动调整权重，使各梯度贡献均衡，无需手动调参。具体地，计算每一损失项关于最后一层网络参数的梯度范数 \(G_i^{(k)}\)，经指数移动平均后取倒数并归一化：
    \[
    \lambda_i^{(k)} = \frac{(\hat{G}_i^{(k)} + \epsilon)^{-1}}{\sum_j (\hat{G}_j^{(k)} + \epsilon)^{-1}}
    \]
  - **训练流程**（算法1）：
    1. **Adam 预热阶段**：从边界可行初始化（最后一层置零），动态采样配点，使用自适应权重更新参数。
    2. **LBFGS 精细阶段**：固定自适应权重，在固定（或定期刷新）的配点集合上进行拟牛顿法精细优化。
- **公式/算法**：论文给出了完整的损失函数（公式16）和两步优化算法（算法1），此处用文字概括如上。

### 3. 实验设计：使用的数据集/场景、benchmark、对比方法

- **数据集/场景**：三个代表性PDE问题：
  1. **椭圆型：Poisson方程**（2D，混合Dirichlet-Neumann边界）
  2. **抛物型：粘性Burgers方程**（1D时间依赖，非零Dirichlet边界）
  3. **双曲型：纯对流方程**（1D时间依赖，周期边界）
  每个问题均有解析解，用于定量对比。
- **Benchmark**：各问题的精确解用于计算相对L2误差和最大边界误差。
- **对比方法**：**HS-PINN**（本文方法） vs. **SPINN**（传统全软PINN），两者使用相同网络架构（5层隐层，每层64神经元，tanh激活）、优化器（Adam + LBFGS）和计算资源。SPINN的损失权重经过手动调优以达最佳表现。

### 4. 资源与算力

- **文中明确点**：训练采用**单精度（torch.float32）在单个GPU上**运行；未标注具体GPU型号。
- **训练时长**：表1给出了壁钟时间（秒）：
  - Poisson：SPINN 315s vs. HS-PINN 123s
  - Burgers：SPINN 170s vs. HS-PINN 128s
  - Convection：SPINN 516s vs. HS-PINN 160s
- **计算代价**：HS-PINN因无需优化Dirichlet惩罚项，收敛所需步数和时间均更少。

### 5. 实验数量与充分性

- **实验数量**：论文报告了**3个基准PDE实验**，每个实验均对比SPINN和HS-PINN。未进行额外的消融实验（如单独验证自适应权重、不同掩码函数、不同网络深度等）。
- **充分性与公平性**：
  - 覆盖了椭圆、抛物、双曲三大类PDE，具有一定的代表性。
  - 对比方法使用相同架构、优化器和充分的手动调参，对比条件公平。
  - 每个实验均提供误差曲线、权重视觉化及最终指标，结果清晰。
  - 不足之处：实验数量较少（仅3个），且缺乏对更复杂（如高维、不规则几何、多物理场）问题的验证。未对自适应权重策略本身进行消融（如对比固定权重、学习率调度等）。客观性方面，论文没有报告多次实验的统计稳定性。

### 6. 论文的主要结论与发现

- **HS-PINN在三种PDE上均显著优于SPINN**：
  - Dirichlet边界被**精确满足至机器精度**（硬约束生效）。
  - 相对L2误差**降低1~2个数量级**：
    - Poisson：1.52×10⁻⁵ → 1.02×10⁻⁷
    - Burgers：5.26×10⁻⁶ → 7.62×10⁻⁸
    - Convection：3.02×10⁻² → 5.75×10⁻⁷
  - 训练时间**减少1.3~3.2倍**，同时收敛更平稳（单调递减）。
  - 双曲对流问题中，HS-PINN精确保持行波相位和幅度，而SPINN出现相位漂移和幅值衰减。
- **自适应逆-份额softmax权重有效平衡了损失分量**，比传统手动调参更稳定，避免训练振荡。
- **结论**：将已知约束以硬约束形式嵌入网络架构，配合自动损失权重，可显著提升PINN求解PDE的准确性、效率和鲁棒性，为物理约束深度学习提供了通用基础。

### 7. 优点

- **方法论创新**：首次将Dirichlet、Neumann、周期边界的处理统一在一个框架内，采用硬-软混合策略，既保证了严格边界满足，又保持了网络灵活性。
- **自适应权重**：提出的逆-份额softmax自动平衡多任务损失，无需手动调节，实用性高。
- **计算高效**：消除了Dirichlet惩罚项的优化负担，显著减少训练时间，且不增加模型复杂度。
- **实验设计清晰**：三个典型PDE涵盖主要类型，对比直接且充分，结果可视化细致（误差分布、损失曲线、权重演化）。
- **可扩展性**：硬约束通过提升/掩码实现，可推广至复杂几何（通过距离函数）和多物理系统。

### 8. 不足与局限

- **实验覆盖面有限**：仅测试三个相对简单的PDE（一维/二维矩形域，平滑解）。未验证：高维问题、不规则几何、非线性耦合系统、异质材料等更实际场景。
- **缺乏消融实验**：没有单独验证硬约束部分和自适应权重部分各自的贡献（例如对比无自适应权重的HS-PINN、或纯硬约束+固定权重等）。
- **未讨论数值稳定性**：在高阶导数（如双调和算子）或极短时间尺度下，硬约束构造的掩码函数可能导致梯度病态，文中未分析。
- **计算资源详细度**：未给出GPU型号、内存占用、具体超参数（如批大小、学习率）等，不利于复现。
- **统计置信度**：每个实验只报告单次运行结果，缺乏多次重复的平均值和标准差，随机性影响未知。
- **适应范围**：当前硬约束方法依赖解析或多项式提升，对于复杂边界形状（如带孔域）需要额外构造距离函数，可能增加复杂度。文末提及未来工作将扩展至不规则域。

（完）
