---
title: Fourier Neural Operators with rank-1 lattice points and hyperbolic cross
title_zh: 基于秩1格点和双曲交叉的傅里叶神经算子
authors: "Jakob Dilen, Alexander Keller, Frances Y. Kuo, Dirk Nuyens"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08871v1"
tags: ["query:geo-phys-ai"]
score: 8.0
evidence: 使用格点改进傅里叶神经算子，可直接应用于地震成像
tldr: 傅里叶神经算子（FNO）通过傅里叶变换学习函数空间映射，但传统张量积网格导致参数冗余和采样效率低。本文提出基于rank-1格点和双曲交叉频率索引集的FNO变体，利用一维快速傅里叶变换简化高维变换。理论分析和实验表明，该方法在更少网络参数、空间点和训练样本下达到更高精度和效率。该工作降低了FNO的架构复杂度，为环面上椭圆偏微分方程求解提供了高效替代方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08871v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1417, \"height\": 666, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08871v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1355, \"height\": 699, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08871v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1434, \"height\": 707, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08871v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1442, \"height\": 1428, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08871v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1435, \"height\": 707, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08871v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1652, \"height\": 710, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08871v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1509, \"height\": 414, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08871v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1197, \"height\": 284, \"label\": \"Table\"}]"
motivation: 现有FNO使用张量积网格导致泛化误差大，需设计更优的空间采样和频率索引策略以提升效率。
method: 采用rank-1格点替代张量积网格进行空间离散，并用双曲交叉频率索引集配合第二个格点作为参数空间训练点，仅需一维FFT。
result: 显著减少网络参数、空间点数和训练样本，同时提高逼近精度与计算效率，在环面椭圆PDE上验证优势。
conclusion: 提出的格基双曲交叉FNO简化了架构，实现了更高效准确的学习，尤其适用于高维问题。
---

## 摘要
\emph{傅里叶神经算子}（FNO）是一种学习函数空间之间映射的神经网络架构。其高效实现基于多维傅里叶变换。通过推导FNO关于空间和参数变量的正则性界，我们证明，用专门构建的秩1格点替换空间张量积网格，并在参数空间中精心构造第二个格点作为训练点，可以改善FNO的泛化误差。我们用更少的网络参数、更少的空间点和更少的训练样本实现了更精确和高效的逼近。此外，架构得到了简化，因为秩1格点上的高维傅里叶变换只需要\emph{一维快速傅里叶变换}，并且我们可以使用带有格点的\emph{双曲交叉}频率索引集。我们展示了基于格点的双曲交叉FNO在环面椭圆偏微分方程上的优势。

## Abstract
The \emph{Fourier neural operator} (FNO) is a neural network architecture that learns mappings between function spaces. Its efficient implementation is based on the multi-dimensional Fourier transform. By deriving general regularity bounds for the FNO with respect to both the spatial and parametric variables, we prove that the generalization error of the FNO can be improved by replacing spatial tensor product grids with purpose-built rank-1 lattice points, and by using a second lattice carefully constructed as training points in the parametric space. We achieve more accurate and efficient approximations from fewer network parameters, fewer spatial points, and fewer training samples. In addition, the architecture is simplified, because the high-dimensional Fourier transform on rank-1 lattices requires only a \emph{one-dimensional fast Fourier transform}, and we can use a \emph{hyperbolic cross} frequency index set with lattice points. We demonstrate the benefits of our \emph{lattice-based hyperbolic-cross FNOs} for an elliptic PDE on the torus.

---

## 论文详细总结（自动生成）

# 论文总结：基于秩1格点和双曲交叉的傅里叶神经算子（Fourier Neural Operators with rank-1 lattice points and hyperbolic cross）

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：传统傅里叶神经算子（FNO）在实现中依赖于张量积网格进行空间离散和傅里叶变换，这在高维情况下（d≥3）导致计算成本高昂，且频率索引集通常取为轴对齐的盒子，包含大量冗余模式。同时，参数空间随机采样导致泛化误差收敛缓慢。
- **动机**：利用拟蒙特卡洛方法（quasi-Monte Carlo）中的秩1格点（rank-1 lattice）和双曲交叉（hyperbolic cross）频域截断来替代传统网格和盒子频率集，以减少网络参数、空间点数和训练样本，同时提高逼近精度和效率。
- **意义**：为FNO提供了一种更简洁、更高效的架构变体，具有严格的理论泛化误差界，并在周期性椭圆PDE上验证了优势，尤其适用于高维或参数化问题。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：
  - 用精心构造的**秩1格点**（rank-1 lattice）替换空间域的张量积网格，使得多维傅里叶变换退化为**一维快速傅里叶变换（1D FFT）**，极大降低计算复杂度。
  - 在参数空间中使用**第二个不同的秩1格点**作为训练点，替代随机采样，依据格点积分理论获得更快的泛化误差收敛率。
  - 频率索引集采用**双曲交叉**（hyperbolic cross）替换盒子，在保持精度的同时大幅减少所需频率模式数量（即网络参数）。

- **关键技术细节**：
  - FNO结构：`Gθ(a)(x) = (Q ∘ T_L ∘ ... ∘ T_1 ∘ P)(a)(x)`，其中每个隐藏层 `T_ℓ(v) = σ(W_ℓ v(x) + (K_ℓ v)(x) + b_ℓ)`，卷积核 `(K_ℓ v)(x) = ∑_{h∈A} bκ_ℓ(h) bv(h) e^{2πi h·x}`。
  - 空间格点：`x_k = (k z mod m)/m`（k=1,...,m），z为生成向量。傅里叶系数近似通过1D FFT计算。
  - 参数空间格点：`y_i = (i w / n + Δ) mod 1`（i=1,...,n），Δ为随机偏移。
  - 随机场模型：仿射模型 `a(x,y) = ψ_0(x) + Σ(y_j - 1/2)ψ_j(x)`；Chebyshev模型 `a(x,y) = ψ_0(x) + Σ sin(2π y_j)ψ_j(x)`。
  - 基函数选择：`ψ_{2j-1}(x) = η e^{2πi h_j·x}/r(h_j)， ψ_{2j}(x) = i η e^{2πi h_j·x}/r(h_j)`，其中 `r(h)` 对应双曲交叉度量。
  - 正则性分析：推导了FNO关于参数y和空间x的混合偏导数上界（定理1、定理3），进而利用格点积分的最坏情况误差界得到泛化误差上界（定理5）。
  - 训练误差与泛化误差的关系：`E_G ≤ tol + O(n^{-r/2} + m^{-(ω-ε)/2})`，其中n为参数训练样本数，m为空间点数，r和ω由光滑性和维度决定。

## 3. 实验设计

- **数据集/场景**：
  - 两个空间维度的环面（torus）上的椭圆PDE：`-∇·(a(x,y)∇u) = f(x)`，周期性边界条件，零均值约束。
  - 输入场a(x,y)采用仿射随机场模型（基函数如上述）。
  - 解算：通过截断傅里叶级数（盒子取M=25）获得高精度参考解。

- **基准**：标准FNO（使用张量积网格 + 盒子频率索引集，M=9，|A|=361）。

- **对比方法**（共4种变体，各5次独立运行）：
  - **FNO**：标准。
  - **FNO-LAT**：盒子索引 + 秩1格点空间。
  - **FNO-HC**：双曲交叉索引 + 张量积网格。
  - **FNO-HC-LAT**：双曲交叉索引 + 秩1格点空间。

- **额外实验**：匹配参数数量实验（FNO M=3 vs FNO-HC-LAT M=9，参数相近）；训练时间分析（CPU上不同M值）。

## 4. 资源与算力

- **GPU**：NVIDIA GeForce P100，batch size 16，2000 epochs，初始学习率1e-2（带平台衰减）。
- **CPU**：用于训练时间研究（图6a）。
- **未明确说明**：GPU数量（可能单卡）、具体CPU型号、内存等信息。论文中仅描述“NVIDIA GeForce P100 GPU”，未提及其他硬件配置。

## 5. 实验数量与充分性

- **实验数量**：
  - 四个主要变体各5次独立运行（共20次训练）。
  - 匹配参数实验：2个模型各5次。
  - 训练时间分析：不同M值（1,3,5,7,9）各4个模型，按M分组测试。
  - 消融：无单独的消融实验文章，但通过对比FNO-LAT、FNO-HC、FNO-HC-LAT分别消融了“格点空间”和“双曲交叉”两个因素。

- **充分性**：
  - 实验覆盖了所提改进的两个核心要素（格点空间+双曲交叉）的单独与组合效果。
  - 匹配参数实验验证了“更少参数达到相似精度”的论点。
  - 训练时间分析证实了计算效率优势。
  - **不足**：仅测试了一个PDE问题（2D环面椭圆型），且仅使用仿射随机场一种模型。未在高维（d≥3）或不同PDE类型（如对流、扩散、波动）上验证。未涉及Chebyshev模型的实验。

## 6. 主要结论与发现

- **精度与效率**：FNO-HC-LAT在相近甚至更少的参数下，达到与标准FNO相当的泛化误差（~2.93e-2 vs ~2.74e-2），参数仅9.44%（表1）。
- **训练时间**：FNO-HC-LAT最快（约54%的时间）。
- **匹配参数时**：FNO-HC-LAT（M=9）与FNO（M=3）误差相近（~2.85e-2 vs ~2.88e-2），参数更少（68.2%）。
- **理论保证**：给出了FNO关于空间和参数正则性的明确上界，结合格点积分理论推导了泛化误差收敛阶（O(n^{-r/2} + m^{-(ω-ε)/2})）。
- **简化计算**：秩1格点使高维FFT退化为1D FFT，降低实现复杂度。

## 7. 优点

- **理论扎实**：首次为FNO提供关于空间变量和参数变量同时分析的严格正则性界，并给出整体泛化误差界。
- **方法创新**：将拟蒙特卡洛（格点）引入FNO的物理空间和参数空间，并搭配双曲交叉频率集，三者协同作用。
- **实验充分**：对每个变体多次独立运行，统计均值与标准差，结果可靠。
- **开源代码**：提供GitHub仓库，便于复现与推广。
- **适用性强**：方法可推广到其他算子学习架构（如DeepONet、PCA-Net等）。

## 8. 不足与局限

- **实验场景局限**：仅测试2D环面上的椭圆PDE，未检验高维（d≥3）、非周期性边界条件、其他类型PDE（如波浪、对流）或真实应用问题。
- **基函数假设**：随机场基函数采用特定傅里叶模式（49式），通用性有待验证。
- **激活函数限制**：理论要求激活函数无穷光滑（如sigmod、tanh、swish），不适用于ReLU，但可用swish近似。
- **忽略非线性升降维**：论文中P和Q取线性，附录虽提及可扩展到非线性，但实验未验证。
- **对比不够全面**：未与最新变体（AFNO、SFNO、DeepONet等）做横向比较。
- **偏差风险**：训练样本和测试样本均来自同一格点构造，可能存在相关性，但使用随机偏移一定程度上缓解。
- **计算资源报告不足**：缺少训练的具体时间（除CPU对比外）、GPU显存占用、数据生成成本等细节。

（完）
